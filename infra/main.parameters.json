{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "databaseLocation": {
      "value": "${AZURE_DATABASE_LOCATION=northeurope}"
    },
    "aiLocation": {
      "value": "${AZURE_AI_LOCATION=swedencentral}"
    },
    "staticWebAppLocation": {
      "value": "${AZURE_SWA_LOCATION=westeurope}"
    },
    "githubToken": {
      "value": "${GITHUB_TOKEN}"
    },
    "mongoAdminUsername": {
      "value": "${MONGO_ADMIN_USERNAME=entangleadmin}"
    },
    "mongoAdminPassword": {
      "value": "${MONGO_ADMIN_PASSWORD}"
    },
    "mongoComputeTier": {
      "value": "${MONGO_COMPUTE_TIER=M30}"
    },
    "mongoStorageGb": {
      "value": "${MONGO_STORAGE_GB=256}"
    },
    "aiModelName": {
      "value": "${AI_MODEL_NAME=gpt-5-mini}"
    },
    "aiModelVersion": {
      "value": "${AI_MODEL_VERSION=2025-08-07}"
    },
    "aiDeploymentName": {
      "value": "${AI_DEPLOYMENT_NAME=gpt-5-mini}"
    },
    "aiDeploymentCapacity": {
      "value": "${AI_DEPLOYMENT_CAPACITY=250}"
    },
    "embeddingModelName": {
      "value": "${AI_EMBEDDING_MODEL=text-embedding-3-small}"
    },
    "embeddingModelVersion": {
      "value": "${AI_EMBEDDING_VERSION=1}"
    },
    "embeddingDeploymentName": {
      "value": "${AI_EMBEDDING_DEPLOYMENT=text-embedding-3-small}"
    },
    "embeddingDeploymentCapacity": {
      "value": "${AI_EMBEDDING_CAPACITY=120}"
    },
    "tavilyApiKey": {
      "value": "${TAVILY_API_KEY=}"
    },
    "apiCpu": {
      "value": "${API_CPU=0.5}"
    },
    "apiMemory": {
      "value": "${API_MEMORY=1.0Gi}"
    },
    "apiMinReplicas": {
      "value": "${API_MIN_REPLICAS=0}"
    },
    "apiMaxReplicas": {
      "value": "${API_MAX_REPLICAS=1}"
    },
    "deployStaging": {
      "value": "${DEPLOY_STAGING=true}"
    },
    "alertEmail": {
      "value": "${ALERT_EMAIL=}"
    }
  }
}
